{
  "name": "aws",
  "version": "0.0.1",
  "private": true,
  "sideEffects": false,
  "engines": {
    "node": ">=v18.17.1"
  },
  "scripts": {
    "sso": "aws sso login --sso-session=svend",
    "env:local:apps:web": "pnpm --filter aws-tools-env env:local:apps:web"
  },
  "packageManager": "pnpm@9.1.4"
}

{
  "name": "aws",
  "version": "0.0.1",
  "private": true,
  "sideEffects": false,
  "engines": {
    "node": ">=v18.17.1"
  },
  "scripts": {
    "sso": "aws sso login --sso-session=svend",
    "env:web:pull:local": "pnpm --filter aws-tools-env web:pull:local"
  },
  "packageManager": "pnpm@9.1.4"
}
